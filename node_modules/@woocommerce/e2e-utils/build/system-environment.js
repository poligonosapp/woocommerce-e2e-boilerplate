"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getEnvironmentContext = void 0;

var _flows = require("./flows");

/**
 * Uses the WooCommerce API to get the environment context.
 */
const getEnvironmentContext = async () => {
  try {
    const environment = await _flows.withRestApi.getSystemEnvironment();
    return {
      wpVersion: environment.wp_version,
      wcVersion: environment.version
    };
  } catch (error) {// Prevent an error here causing tests to fail.
  }
};

exports.getEnvironmentContext = getEnvironmentContext;
//# sourceMappingURL=system-environment.js.map