"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.variationFactory = variationFactory;

var _api = require("@woocommerce/api");

var _fishery = require("fishery");

/**
 * Creates a new factory for creating a product variation.
 *
 * @param {HTTPClient} httpClient The HTTP client we will give the repository.
 * @return {AsyncFactory} The factory for creating models.
 */
function variationFactory(httpClient) {
  const repository = _api.ProductVariation.restRepository(httpClient);

  return _fishery.Factory.define(_ref => {
    let {
      params,
      onCreate
    } = _ref;
    const {
      productId,
      variation
    } = params;
    onCreate(model => {
      return repository.create(productId, model);
    });
    return variation;
  });
}
//# sourceMappingURL=variation.js.map